<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".HomeActivity">
      <item android:id="@+id/action_home_share"
            android:icon="@mipmap/ic_share_menu"
            android:title="@string/menu_home_share"
            app:showAsAction="ifRoom"/>
    <item android:id="@+id/action_home_search"
          android:icon="@mipmap/ic_search"
          android:title="@string/menu_home_search"
          app:showAsAction="ifRoom"/>
      <item android:id="@+id/action_home_rank"
            android:icon="@mipmap/ic_rank"
            android:title="@string/menu_home_rank"
            app:showAsAction="ifRoom"/>
</menu>
